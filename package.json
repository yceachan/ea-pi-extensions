{
  "name": "ea-pi-extensions",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "gcm": "./gcm",
    "mono-sync": "bun scripts/mono-sync.mjs",
    "mono-release": "bun scripts/mono-release.mjs",
    "version:check": "bun scripts/mono-sync.mjs --check",
    "version:sync": "bun scripts/mono-sync.mjs --sync",
    "version:reset": "bun scripts/mono-sync.mjs --reset",
    "typecheck": "bun run --filter '*' typecheck",
    "gbump": "./gbump"
  },
  "private": true
}
